name: skills-validate
description: Validate Threadroot skill frontmatter, names, links, eval triggers,
  and progressive-disclosure hygiene.
scope: project
risk: low
confirm: false
healthcheck:
  run: threadroot --version
  expectExitCode: 0
input: {}
run: threadroot skills validate
