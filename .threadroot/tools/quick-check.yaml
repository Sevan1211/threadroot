name: quick-check
description: "Run the normal local development gate: typecheck, lint, and tests."
scope: project
risk: low
confirm: false
healthcheck:
  run: pnpm --version
  expectExitCode: 0
input: {}
run: pnpm typecheck && pnpm lint && pnpm test
