{
  "shouldTrigger": [
    "Add a new bootstrap flag and update the website prompt.",
    "Rename a Threadroot command and make sure agents stop using the old one.",
    "Change JSON output for threadroot start."
  ],
  "shouldNotTrigger": [
    "Refactor an internal helper without touching CLI behavior.",
    "Update only a private test fixture."
  ]
}
